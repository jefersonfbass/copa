export interface Benefit {
  id: string;
  title: string;
  description: string;
  iconName: string;
}

export interface ColoringPage {
  id: string;
  title: string;
  imageSrc: string;
  description: string;
}
